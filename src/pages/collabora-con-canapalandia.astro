---
import Layout from "../layouts/MigratedLayout.astro";

const canonical = "https://canapalandia.com/collabora-con-canapalandia/";
const contactEmail = "info@canapalandia.com";
const contactSubject = "Proposta collaborazione editoriale Canapalandia";
const mailtoHref = `mailto:${contactEmail}?subject=${encodeURIComponent(contactSubject)}`;

const metadata = {
  title: "Collabora con Canapalandia | Partnership editoriali",
  description:
    "Proponi una collaborazione editoriale a Canapalandia: guest post, contenuti sponsorizzati, interviste e approfondimenti selezionati su canapa, CBD, normativa e filiera.",
  canonical,
  robots: {
    index: true,
    follow: true,
  },
  openGraph: {
    title: "Collabora con Canapalandia | Partnership editoriali",
    description:
      "Valutiamo proposte editoriali selezionate con brand, professionisti, associazioni e realtà coerenti con il progetto Canapalandia.",
    type: "website",
    url: canonical,
    siteName: "Canapalandia",
  },
  twitter: {
    card: "summary_large_image",
    title: "Collabora con Canapalandia | Partnership editoriali",
    description:
      "Guest post, contenuti sponsorizzati, interviste e approfondimenti selezionati su canapa, CBD, normativa e filiera.",
  },
};
---

<Layout metadata={metadata}>
  <div class="collab-page">
    <section class="collab-hero" aria-labelledby="collab-title">
      <div class="collab-shell collab-hero__grid">
        <div class="collab-hero__copy">
          <p class="collab-eyebrow">Partnership editoriali selezionate</p>
          <h1 id="collab-title">Collaborazioni che aggiungono valore, non solo visibilità.</h1>
          <p class="collab-hero__lead">
            Canapalandia valuta proposte con brand, professionisti, associazioni e progetti coerenti con la propria linea editoriale. Guest post, contenuti sponsorizzati, interviste e approfondimenti vengono considerati solo quando hanno qualcosa di utile da offrire ai lettori.
          </p>

          <div class="collab-actions" aria-label="Azioni principali">
            <a class="collab-button collab-button--primary" href="#form-collaborazione">
              Invia una proposta
            </a>
            <a class="collab-button collab-button--secondary" href="#criteri-editoriali">
              Leggi i criteri
            </a>
          </div>
        </div>

        <aside class="collab-hero__panel" aria-labelledby="filtri-title">
          <p class="collab-panel__label">Prima di scriverci</p>
          <h2 id="filtri-title">Una proposta funziona se supera tre filtri.</h2>
          <ul class="collab-checklist">
            <li>
              <strong>Coerenza</strong>
              <span>Il tema deve essere pertinente a Canapalandia e al suo pubblico.</span>
            </li>
            <li>
              <strong>Utilità</strong>
              <span>Il contenuto deve informare, spiegare o offrire un punto di vista reale.</span>
            </li>
            <li>
              <strong>Trasparenza</strong>
              <span>Le collaborazioni commerciali vengono indicate in modo chiaro.</span>
            </li>
          </ul>
        </aside>
      </div>
    </section>

    <section class="collab-proof" aria-label="Principi della collaborazione">
      <div class="collab-shell collab-proof__grid">
        <div>
          <strong>Selezione manuale</strong>
          <span>Ogni proposta viene letta e valutata.</span>
        </div>
        <div>
          <strong>Linea editoriale indipendente</strong>
          <span>La collaborazione non garantisce la pubblicazione.</span>
        </div>
        <div>
          <strong>Niente link building pura</strong>
          <span>Il contenuto deve avere valore anche senza il link.</span>
        </div>
      </div>
    </section>

    <section class="collab-section" aria-labelledby="formati-title">
      <div class="collab-shell">
        <div class="collab-section__intro">
          <p class="collab-kicker">Formati</p>
          <h2 id="formati-title">Cosa possiamo valutare</h2>
          <p>
            Non esiste un formato obbligatorio. Conta soprattutto la qualità della proposta, la pertinenza del tema e la chiarezza dell’obiettivo.
          </p>
        </div>

        <div class="collab-cards">
          <article class="collab-card">
            <span class="collab-card__index" aria-hidden="true">01</span>
            <h3>Guest post</h3>
            <p>Articoli originali e informativi, già scritti o da sviluppare insieme, sottoposti a revisione editoriale prima della pubblicazione.</p>
          </article>
          <article class="collab-card">
            <span class="collab-card__index" aria-hidden="true">02</span>
            <h3>Contenuti sponsorizzati</h3>
            <p>Progetti commerciali coerenti con Canapalandia, pubblicati con una dichiarazione trasparente della collaborazione.</p>
          </article>
          <article class="collab-card">
            <span class="collab-card__index" aria-hidden="true">03</span>
            <h3>Interviste</h3>
            <p>Conversazioni con aziende, associazioni, professionisti e realtà che abbiano esperienza, dati o prospettive interessanti da condividere.</p>
          </article>
          <article class="collab-card">
            <span class="collab-card__index" aria-hidden="true">04</span>
            <h3>Approfondimenti</h3>
            <p>Contenuti su canapa, CBD, normativa, filiera, sostenibilità, innovazione, tracciabilità e cultura del settore.</p>
          </article>
        </div>
      </div>
    </section>

    <section id="criteri-editoriali" class="collab-section collab-section--soft scroll-mt-24" aria-labelledby="criteri-title">
      <div class="collab-shell collab-criteria">
        <div class="collab-criteria__copy">
          <p class="collab-kicker">Criteri editoriali</p>
          <h2 id="criteri-title">Prima viene il contenuto. Poi la collaborazione.</h2>
          <p>
            Proteggere la credibilità del progetto significa dire alcuni no in modo chiaro. Valutiamo proposte che possano stare su Canapalandia anche dal punto di vista editoriale, non soltanto commerciale.
          </p>
        </div>

        <div class="collab-criteria__columns">
          <section class="collab-criteria__box collab-criteria__box--yes" aria-labelledby="cerchiamo-title">
            <h3 id="cerchiamo-title">Cerchiamo</h3>
            <ul>
              <li>Argomenti pertinenti e ben delimitati.</li>
              <li>Fonti, esperienza o competenze reali.</li>
              <li>Testi originali e revisionabili.</li>
              <li>Proposte chiare su obiettivo e pubblico.</li>
              <li>Trasparenza su eventuali finalità commerciali.</li>
            </ul>
          </section>

          <section class="collab-criteria__box collab-criteria__box--no" aria-labelledby="non-accettiamo-title">
            <h3 id="non-accettiamo-title">Non accettiamo</h3>
            <ul>
              <li>Richieste pensate solo per ottenere backlink.</li>
              <li>Claim medici o legali non verificabili.</li>
              <li>Contenuti copiati, fuorvianti o superficiali.</li>
              <li>Inviti ad aggirare o ignorare la normativa.</li>
              <li>Testi generati automaticamente senza revisione reale.</li>
            </ul>
          </section>
        </div>
      </div>
    </section>

    <section class="collab-section collab-section--dark" aria-labelledby="processo-title">
      <div class="collab-shell">
        <div class="collab-section__intro collab-section__intro--light">
          <p class="collab-kicker">Processo</p>
          <h2 id="processo-title">Come funziona</h2>
          <p>Il processo è semplice e volutamente selettivo. Non pubblichiamo automaticamente ciò che riceviamo.</p>
        </div>

        <ol class="collab-process">
          <li>
            <span aria-hidden="true">1</span>
            <div>
              <h3>Invii la proposta</h3>
              <p>Ci racconti chi sei, il tema, il formato e l’obiettivo della collaborazione.</p>
            </div>
          </li>
          <li>
            <span aria-hidden="true">2</span>
            <div>
              <h3>La valutiamo</h3>
              <p>Controlliamo pertinenza, utilità, qualità e compatibilità con la linea editoriale.</p>
            </div>
          </li>
          <li>
            <span aria-hidden="true">3</span>
            <div>
              <h3>Definiamo i dettagli</h3>
              <p>Se la proposta è adatta, concordiamo formato, revisione, trasparenza ed eventuali condizioni economiche.</p>
            </div>
          </li>
        </ol>
      </div>
    </section>

    <section id="form-collaborazione" class="collab-section collab-form-section scroll-mt-24" aria-labelledby="form-title">
      <div class="collab-shell collab-form-layout">
        <div class="collab-form-intro">
          <p class="collab-kicker">Proposta</p>
          <h2 id="form-title">Raccontaci cosa vuoi costruire insieme.</h2>
          <p>
            Bastano informazioni precise. Ci interessa capire chi sei, cosa proponi e perché potrebbe essere utile ai lettori di Canapalandia.
          </p>

          <div class="collab-form-note" role="note">
            <strong>Valutazione manuale</strong>
            <p>L’invio del modulo non implica accettazione o pubblicazione.</p>
          </div>

          <p class="collab-direct-contact">
            Preferisci l’email? <a href={mailtoHref}>{contactEmail}</a>
          </p>
        </div>

        <form class="collaboration-form" method="POST" action="/api/collaborazione">
          <p id="required-note" class="form-required-note">I campi con * sono obbligatori.</p>

          <input class="honeypot-field" type="text" name="_gotcha" tabindex="-1" autocomplete="off" aria-hidden="true" />

          <fieldset aria-describedby="required-note">
            <legend>Chi sei</legend>
            <div class="form-grid">
              <div class="field-group">
                <label for="collab-nome">Nome e cognome <span aria-hidden="true">*</span></label>
                <input id="collab-nome" name="nome_cognome" type="text" autocomplete="name" required />
              </div>
              <div class="field-group">
                <label for="collab-email">Email <span aria-hidden="true">*</span></label>
                <input id="collab-email" name="email" type="email" autocomplete="email" required />
              </div>
              <div class="field-group">
                <label for="collab-azienda">Azienda, brand o progetto <span aria-hidden="true">*</span></label>
                <input id="collab-azienda" name="azienda" type="text" autocomplete="organization" required />
              </div>
              <div class="field-group">
                <label for="collab-sito">Sito web <span aria-hidden="true">*</span></label>
                <input id="collab-sito" name="sito_web" type="url" inputmode="url" placeholder="https://esempio.com" required />
              </div>
            </div>
          </fieldset>

          <fieldset aria-describedby="required-note">
            <legend>La proposta</legend>
            <div class="form-grid">
              <div class="field-group">
                <label for="collab-tipo">Tipo di collaborazione <span aria-hidden="true">*</span></label>
                <select id="collab-tipo" name="tipo_contenuto" required>
                  <option value="" disabled selected>Seleziona un’opzione</option>
                  <option value="Guest post">Guest post</option>
                  <option value="Contenuto sponsorizzato">Contenuto sponsorizzato</option>
                  <option value="Intervista">Intervista</option>
                  <option value="Approfondimento tematico">Approfondimento tematico</option>
                  <option value="Altro">Altro</option>
                </select>
              </div>
              <div class="field-group">
                <label for="collab-argomento">Titolo o tema proposto <span aria-hidden="true">*</span></label>
                <input id="collab-argomento" name="argomento" type="text" placeholder="Es. Tracciabilità nella filiera della canapa" required />
              </div>
              <div class="field-group field-full">
                <label for="collab-link">Link o materiale da valutare <span aria-hidden="true">*</span></label>
                <input id="collab-link" name="link" type="url" inputmode="url" aria-describedby="link-help" placeholder="https://esempio.com/pagina" required />
                <small id="link-help">Può essere il sito, una bozza online, una pagina prodotto o un contenuto di riferimento.</small>
              </div>
              <div class="field-group field-full">
                <label for="collab-messaggio">Descrivi proposta e obiettivo <span aria-hidden="true">*</span></label>
                <textarea id="collab-messaggio" name="messaggio" rows="7" placeholder="Spiega cosa vorresti proporre, perché è adatto a Canapalandia e quale valore può offrire ai lettori." required></textarea>
              </div>
            </div>
          </fieldset>

          <div class="privacy-box">
            <label>
              <input type="checkbox" name="privacy" value="accettata" required />
              <span>
                Ho letto e accetto <a href="/privacy-policy/" target="_blank" rel="noopener noreferrer">l’informativa privacy</a> e acconsento al trattamento dei dati per la valutazione della proposta editoriale. <span aria-hidden="true">*</span>
              </span>
            </label>
          </div>

          <div class="form-actions">
            <button type="submit">Invia la proposta</button>
          </div>
        </form>
      </div>
    </section>

    <section class="collab-section collab-faq" aria-labelledby="faq-title">
      <div class="collab-shell collab-faq__layout">
        <div>
          <p class="collab-kicker">FAQ</p>
          <h2 id="faq-title">Prima di inviare</h2>
        </div>
        <div class="collab-faq__list">
          <details>
            <summary>Pubblicate qualsiasi guest post?</summary>
            <p>No. Ogni proposta viene valutata per pertinenza, qualità, originalità e utilità per chi legge.</p>
          </details>
          <details>
            <summary>Accettate contenuti sponsorizzati?</summary>
            <p>Sì, quando sono coerenti con la linea editoriale. La natura commerciale della collaborazione viene dichiarata con trasparenza.</p>
          </details>
          <details>
            <summary>Posso chiedere solo l’inserimento di un link?</summary>
            <p>In generale no. Le richieste esclusivamente orientate al link building non rientrano nel tipo di collaborazione che cerchiamo.</p>
          </details>
          <details>
            <summary>La proposta garantisce la pubblicazione?</summary>
            <p>No. Canapalandia mantiene piena autonomia editoriale e può chiedere modifiche, proporre un formato diverso o rifiutare una collaborazione.</p>
          </details>
        </div>
      </div>
    </section>

    <section class="collab-editorial-note" aria-label="Trasparenza editoriale">
      <div class="collab-shell">
        <p>
          <strong>Trasparenza editoriale.</strong> Una collaborazione commerciale non equivale a una raccomandazione, certificazione o approvazione dei prodotti o servizi citati. Canapalandia può modificare titoli, struttura, anchor text e numero di link quando necessario per mantenere qualità e coerenza editoriale.
        </p>
      </div>
    </section>
  </div>
</Layout>

<style>
  .collab-page {
    --green: #173f2d;
    --green-dark: #0f2f21;
    --green-hover: #20543d;
    --ink: #18231d;
    --muted: #4d5a52;
    --cream: #f7f3e8;
    --paper: #fffdf7;
    --line: #cdd7d0;
    --accent: #f6dfa0;
    --accent-hover: #ffe9ad;
    --focus-light: #fff7d6;
    --focus-dark: #173f2d;
    color: var(--ink);
    background: var(--paper);
  }

  .collab-shell {
    width: min(1120px, calc(100% - 2.5rem));
    margin-inline: auto;
  }

  .collab-hero {
    background: linear-gradient(135deg, #0d2b1e 0%, #173f2d 66%, #20543d 100%);
    color: #fff;
    padding: clamp(4rem, 8vw, 7rem) 0;
  }

  .collab-hero__grid {
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(300px, 0.75fr);
    gap: clamp(2.5rem, 6vw, 5rem);
    align-items: center;
  }

  .collab-eyebrow,
  .collab-kicker {
    margin: 0;
    font: 800 0.78rem/1.3 "Archivo", system-ui, sans-serif;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .collab-eyebrow { color: #ffe8a7; }
  .collab-kicker { color: var(--green-hover); }

  .collab-hero h1,
  .collab-section h2,
  .collab-form-intro h2,
  .collab-faq h2 {
    font-family: "Newsreader", Georgia, serif;
    font-weight: 600;
    text-wrap: balance;
  }

  .collab-hero h1 {
    max-width: 15ch;
    margin: 1rem 0 0;
    font-size: clamp(3rem, 6.5vw, 6rem);
    line-height: 0.98;
    letter-spacing: -0.04em;
    color: #fffdf7;
  }

  .collab-hero__lead {
    max-width: 52rem;
    margin: 1.75rem 0 0;
    color: #e7efe9;
    font-size: clamp(1.08rem, 2vw, 1.28rem);
    line-height: 1.68;
  }

  .collab-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.9rem;
    margin-top: 2.2rem;
  }

  .collab-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 3.25rem;
    min-width: 11rem;
    padding: 0.8rem 1.45rem;
    border: 2px solid transparent;
    border-radius: 0.8rem;
    font-size: 1rem;
    font-weight: 800;
    line-height: 1.2;
    text-align: center;
    text-decoration: none !important;
    cursor: pointer;
  }

  .collab-page .collab-button--primary {
    background: var(--accent) !important;
    color: #102d20 !important;
    border-color: var(--accent) !important;
    box-shadow: 0 10px 26px rgba(0, 0, 0, 0.2);
  }

  .collab-page .collab-button--primary:hover {
    background: var(--accent-hover) !important;
    border-color: var(--accent-hover) !important;
  }

  .collab-page .collab-button--secondary {
    background: transparent !important;
    color: #fff !important;
    border-color: rgba(255, 255, 255, 0.78) !important;
  }

  .collab-page .collab-button--secondary:hover {
    background: rgba(255, 255, 255, 0.11) !important;
    border-color: #fff !important;
  }

  .collab-button:focus-visible {
    outline: 3px solid var(--focus-light);
    outline-offset: 4px;
  }

  .collab-hero__panel {
    padding: clamp(1.5rem, 4vw, 2.25rem);
    border: 1px solid rgba(255, 255, 255, 0.32);
    border-radius: 1.25rem;
    background: rgba(255, 255, 255, 0.08);
  }

  .collab-panel__label {
    margin: 0;
    color: #ffe8a7;
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .collab-hero__panel h2 {
    margin: 0.8rem 0 0;
    color: #fff;
    font-size: clamp(1.5rem, 3vw, 2rem);
    line-height: 1.2;
  }

  .collab-checklist {
    list-style: none;
    margin: 1.4rem 0 0;
    padding: 0;
  }

  .collab-checklist li {
    display: grid;
    gap: 0.35rem;
    padding: 1rem 0;
    border-top: 1px solid rgba(255, 255, 255, 0.22);
  }

  .collab-checklist strong { color: #fff; font-size: 1rem; }
  .collab-checklist span { color: #e4ece7; font-size: 0.95rem; line-height: 1.55; }

  .collab-proof {
    border-bottom: 1px solid var(--line);
    background: var(--paper);
  }

  .collab-proof__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .collab-proof__grid > div {
    padding: 1.6rem;
    border-left: 1px solid var(--line);
  }

  .collab-proof__grid > div:last-child { border-right: 1px solid var(--line); }
  .collab-proof strong, .collab-proof span { display: block; }
  .collab-proof strong { color: var(--green-dark); font-size: 0.98rem; }
  .collab-proof span { margin-top: 0.35rem; color: var(--muted); font-size: 0.9rem; line-height: 1.55; }

  .collab-section { padding: clamp(4.5rem, 8vw, 7rem) 0; }
  .collab-section--soft { background: var(--cream); }
  .collab-section--dark { background: var(--green-dark); color: #fff; }

  .collab-section__intro { max-width: 50rem; }
  .collab-section__intro h2,
  .collab-criteria__copy h2,
  .collab-form-intro h2,
  .collab-faq h2 {
    margin: 0.8rem 0 0;
    color: var(--green-dark);
    font-size: clamp(2.5rem, 5vw, 4.4rem);
    line-height: 1;
    letter-spacing: -0.035em;
  }

  .collab-section__intro > p:last-child,
  .collab-criteria__copy > p:last-child,
  .collab-form-intro > p,
  .collab-faq__list p {
    color: var(--muted);
    font-size: 1.04rem;
    line-height: 1.72;
  }

  .collab-section__intro--light h2,
  .collab-section__intro--light .collab-kicker { color: #fff; }
  .collab-section__intro--light > p:last-child { color: #e4ece7; }

  .collab-cards {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
    margin-top: 3rem;
  }

  .collab-card {
    min-height: 14rem;
    padding: 1.7rem;
    border: 1px solid var(--line);
    border-radius: 1rem;
    background: #fff;
  }

  .collab-card__index { color: #63746a; font-size: 0.78rem; font-weight: 800; letter-spacing: 0.12em; }
  .collab-card h3 { margin: 2rem 0 0; color: var(--green-dark); font-size: 1.4rem; line-height: 1.2; }
  .collab-card p { margin: 0.8rem 0 0; color: var(--muted); font-size: 0.98rem; line-height: 1.67; }

  .collab-criteria {
    display: grid;
    grid-template-columns: minmax(0, 0.85fr) minmax(0, 1.15fr);
    gap: clamp(2.5rem, 6vw, 5rem);
    align-items: start;
  }

  .collab-criteria__columns {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
  }

  .collab-criteria__box {
    padding: 1.6rem;
    border: 1px solid var(--line);
    border-radius: 1rem;
    background: #fff;
  }

  .collab-criteria__box--yes { border-top: 4px solid #4f7b58; }
  .collab-criteria__box--no { border-top: 4px solid #9a5d49; }
  .collab-criteria__box h3 { margin: 0; color: var(--green-dark); font-size: 1rem; }
  .collab-criteria__box ul { margin: 1.15rem 0 0; padding-left: 1.2rem; }
  .collab-criteria__box li { margin-top: 0.7rem; color: #3f4c44; line-height: 1.58; }

  .collab-process {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1rem;
    list-style: none;
    margin: 3rem 0 0;
    padding: 0;
  }

  .collab-process li {
    padding: 1.7rem;
    border: 1px solid rgba(255, 255, 255, 0.28);
    border-radius: 1rem;
    background: rgba(255, 255, 255, 0.06);
  }

  .collab-process li > span {
    display: grid;
    place-items: center;
    width: 2.4rem;
    height: 2.4rem;
    border-radius: 999px;
    background: var(--accent);
    color: var(--green-dark);
    font-weight: 850;
  }

  .collab-process h3 { margin: 1.25rem 0 0; color: #fff; font-size: 1.25rem; }
  .collab-process p { margin: 0.65rem 0 0; color: #e4ece7; font-size: 0.96rem; line-height: 1.62; }

  .collab-form-section { background: #f1eee5; }

  .collab-form-layout {
    display: grid;
    grid-template-columns: minmax(0, 0.72fr) minmax(0, 1.28fr);
    gap: clamp(2.5rem, 6vw, 5rem);
    align-items: start;
  }

  .collab-form-intro { position: sticky; top: 7rem; }

  .collab-form-note {
    margin-top: 2rem;
    padding: 1rem 1.1rem;
    border-left: 4px solid #b88e25;
    background: #fff;
  }

  .collab-form-note strong { color: var(--green-dark); }
  .collab-form-note p { margin: 0.35rem 0 0; color: var(--muted); line-height: 1.55; }

  .collab-direct-contact { margin-top: 1.4rem !important; }
  .collab-direct-contact a,
  .privacy-box a {
    color: #0f5436 !important;
    font-weight: 800;
    text-decoration: underline;
    text-decoration-thickness: 2px;
    text-underline-offset: 0.18em;
  }

  .collab-direct-contact a:focus-visible,
  .privacy-box a:focus-visible,
  .collab-faq summary:focus-visible {
    outline: 3px solid var(--focus-dark);
    outline-offset: 3px;
    border-radius: 0.25rem;
  }

  .collaboration-form {
    display: grid;
    gap: 1.8rem;
    padding: clamp(1.4rem, 4vw, 2.4rem);
    border: 1px solid #c8d2cb;
    border-radius: 1rem;
    background: #fff;
    box-shadow: 0 18px 48px rgba(23, 63, 45, 0.09);
  }

  .form-required-note { margin: 0; color: #4d5a52; font-size: 0.9rem; }
  .collaboration-form fieldset { min-width: 0; margin: 0; padding: 0; border: 0; }
  .collaboration-form legend {
    width: 100%;
    margin: 0 0 1.15rem;
    padding: 0 0 0.8rem;
    border-bottom: 1px solid var(--line);
    color: var(--green-dark);
    font-size: 0.9rem;
    font-weight: 850;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .form-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 1.2rem; }
  .field-full { grid-column: 1 / -1; }
  .field-group { display: flex; flex-direction: column; gap: 0.5rem; }
  .field-group label { color: #26352c; font-size: 0.94rem; font-weight: 800; }
  .field-group label > span { color: #8a3f2c; }
  .field-group small { color: #56645c; font-size: 0.82rem; line-height: 1.48; }

  .collaboration-form input:not(.honeypot-field),
  .collaboration-form select,
  .collaboration-form textarea {
    width: 100%;
    box-sizing: border-box;
    min-height: 3.15rem;
    border: 2px solid #b9c6bd;
    border-radius: 0.6rem;
    background: #fff;
    color: #17231c;
    padding: 0.82rem 0.95rem;
    font: inherit;
    font-size: 1rem;
    line-height: 1.45;
  }

  .collaboration-form input:not(.honeypot-field)::placeholder,
  .collaboration-form textarea::placeholder { color: #69766e; opacity: 1; }

  .collaboration-form input:not(.honeypot-field):focus,
  .collaboration-form select:focus,
  .collaboration-form textarea:focus {
    border-color: var(--green-dark);
    outline: 3px solid rgba(23, 63, 45, 0.2);
    outline-offset: 2px;
  }

  .collaboration-form textarea { min-height: 11rem; resize: vertical; }

  .honeypot-field {
    position: absolute !important;
    left: -9999px !important;
    width: 1px !important;
    height: 1px !important;
    overflow: hidden !important;
    opacity: 0 !important;
    pointer-events: none !important;
  }

  .privacy-box {
    padding: 1rem;
    border: 1px solid #cbd5ce;
    border-radius: 0.7rem;
    background: #f5f7f4;
  }

  .privacy-box label {
    display: flex;
    align-items: flex-start;
    gap: 0.8rem;
    color: #3f4c44;
    font-size: 0.9rem;
    line-height: 1.58;
  }

  .privacy-box input[type="checkbox"] {
    width: 1.35rem;
    height: 1.35rem;
    margin-top: 0.1rem;
    flex: 0 0 auto;
    accent-color: var(--green-dark);
  }

  .privacy-box input[type="checkbox"]:focus-visible {
    outline: 3px solid rgba(23, 63, 45, 0.28);
    outline-offset: 3px;
  }

  .form-actions button {
    min-height: 3.25rem;
    border: 2px solid var(--green-dark);
    border-radius: 0.7rem;
    background: var(--green-dark);
    color: #fff;
    padding: 0.8rem 1.5rem;
    font-size: 1rem;
    font-weight: 850;
    cursor: pointer;
  }

  .form-actions button:hover { background: var(--green-hover); border-color: var(--green-hover); }
  .form-actions button:focus-visible { outline: 3px solid var(--green-dark); outline-offset: 4px; }

  .collab-faq { background: var(--paper); }
  .collab-faq__layout { display: grid; grid-template-columns: minmax(220px, 0.6fr) minmax(0, 1.4fr); gap: clamp(2rem, 6vw, 5rem); }
  .collab-faq__list { border-top: 1px solid var(--line); }
  .collab-faq details { border-bottom: 1px solid var(--line); }
  .collab-faq summary {
    position: relative;
    min-height: 3.5rem;
    display: flex;
    align-items: center;
    padding: 1rem 2.2rem 1rem 0;
    list-style: none;
    cursor: pointer;
    color: var(--green-dark);
    font-weight: 800;
  }
  .collab-faq summary::-webkit-details-marker { display: none; }
  .collab-faq summary::after { content: "+"; position: absolute; right: 0.25rem; font-size: 1.45rem; }
  .collab-faq details[open] summary::after { content: "−"; }
  .collab-faq__list p { margin: -0.2rem 0 1.35rem; max-width: 44rem; font-size: 0.98rem; }

  .collab-editorial-note { padding: 1.7rem 0; border-top: 1px solid var(--line); background: #e9eee9; }
  .collab-editorial-note p { margin: 0; color: #445149; font-size: 0.86rem; line-height: 1.65; }
  .collab-editorial-note strong { color: var(--green-dark); }

  @media (max-width: 900px) {
    .collab-hero__grid,
    .collab-criteria,
    .collab-form-layout,
    .collab-faq__layout { grid-template-columns: 1fr; }
    .collab-form-intro { position: static; }
    .collab-process { grid-template-columns: 1fr; }
  }

  @media (max-width: 700px) {
    .collab-shell { width: min(100% - 1.5rem, 1120px); }
    .collab-proof__grid,
    .collab-cards,
    .collab-criteria__columns,
    .form-grid { grid-template-columns: 1fr; }

    .collab-proof__grid > div,
    .collab-proof__grid > div:last-child {
      border-left: 0;
      border-right: 0;
      border-top: 1px solid var(--line);
      padding-inline: 0;
    }
    .collab-proof__grid > div:first-child { border-top: 0; }
    .field-full { grid-column: auto; }
    .collab-actions { flex-direction: column; align-items: stretch; }
    .collab-button, .form-actions button { width: 100%; }
    .collab-hero h1 { font-size: clamp(2.8rem, 14vw, 4.5rem); }
  }

  @media (prefers-reduced-motion: reduce) {
    *, *::before, *::after { scroll-behavior: auto !important; transition-duration: 0.01ms !important; animation-duration: 0.01ms !important; }
  }
</style>
