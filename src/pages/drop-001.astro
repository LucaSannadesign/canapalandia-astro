---
import Layout from "../layouts/MigratedLayout.astro";

const creativeVersion = "neutral-format-v1";

const products = [
  {
    id: "tshirt",
    eyebrow: "01 · Wear",
    name: "T-shirt Bella+Canvas 3001",
    concept: "CONCEPT 01",
    price: "€29,90",
    launchPrice: "€26,90",
    saving: "Risparmi €3,00",
    note: "Stampa frontale. Nel demand test il visual è un placeholder neutro: misuriamo soprattutto formato e fascia prezzo.",
  },
  {
    id: "poster",
    eyebrow: "02 · Print",
    name: "Poster 21 × 30 cm",
    concept: "CONCEPT 02",
    price: "€19,90",
    launchPrice: "€17,90",
    saving: "Risparmi €2,00",
    note: "Manifesto editoriale compatto. Il visual definitivo verrà progettato solo dopo i controlli IP e compliance.",
  },
  {
    id: "tote",
    eyebrow: "03 · Carry",
    name: "Tote BagBase W101",
    concept: "CONCEPT 03",
    price: "€24,90",
    launchPrice: "€21,90",
    saving: "Risparmi €3,00",
    note: "Shopper 38 × 42 cm. L'area DTF definitiva sarà validata nel Product Designer prima di qualunque produzione.",
  },
];
---

<Layout
  title="Drop 001 — Test di interesse | Canapalandia"
  description="Test sperimentale di formato e prezzo per tre prodotti editoriali. Nessun checkout, ordine o pagamento."
  canonical="/drop-001/"
  robots="noindex,nofollow,noarchive"
  newsletter={false}
>
  <main class="drop-page">
    <section class="drop-hero" aria-labelledby="drop-title">
      <p class="kicker">DROP 001 · FORMAT / PRICE TEST</p>
      <h1 id="drop-title">Drop 001</h1>
      <p class="lead">
        Tre formati, tre fasce prezzo, nessun checkout. Questo test misura solo
        l'interesse prima di progettare un prodotto reale.
      </p>
      <div class="status-note" role="note">
        <strong>Prototype / not for sale.</strong>
        I visual sono placeholder neutri: non rappresentano marchi o slogan destinati
        alla vendita. Nessun pagamento, ordine o impegno di acquisto.
      </div>
    </section>

    <section class="intro" aria-labelledby="intro-title">
      <p class="index">001 / INTEREST TEST</p>
      <div>
        <h2 id="intro-title">Quale formato compreresti più facilmente?</h2>
        <p>
          Confrontiamo intenzione di acquisto, fascia prezzo e formato. Per non anticipare
          la clearance IP, i tre visual mostrano soltanto identificatori neutri e non
          testano slogan, marchi o riferimenti a cannabis, marijuana o 420.
        </p>
        <div class="launch-rule">
          <strong>Vantaggio waitlist:</strong> se in futuro un prodotto reale supererà
          tutti i gate, la lista riceverà l'eventuale comunicazione di lancio e il prezzo
          dedicato.
        </div>
      </div>
    </section>

    <section class="product-grid" aria-label="Prodotti candidati Drop 001">
      {products.map((product) => (
        <article class="product-card" data-product-card={product.id}>
          <div class="artwork" aria-label={`Placeholder grafico ${product.concept}`}>
            <span class="artwork-series">DROP 001 / FORMAT TEST</span>
            <strong>{product.concept}</strong>
            <span class="artwork-foot">NEUTRAL PLACEHOLDER · NOT FOR SALE</span>
          </div>

          <div class="product-body">
            <p class="product-eyebrow">{product.eyebrow}</p>
            <h3>{product.name}</h3>

            <div class="pricing">
              <p class="reference-price">
                <span>Prezzo previsto</span>
                <strong>{product.price}</strong>
              </p>
              <p class="launch-price">
                {product.launchPrice}
                <span>prezzo indicativo waitlist</span>
              </p>
              <p class="saving">{product.saving}</p>
            </div>

            <p class="product-note">{product.note}</p>
            <button
              class="interest-button"
              type="button"
              data-product={product.id}
              data-product-name={product.name}
              data-concept={product.concept}
            >
              Mi interessa questo formato
            </button>
          </div>
        </article>
      ))}
    </section>

    <section class="waitlist" id="waitlist" aria-labelledby="waitlist-title">
      <div class="waitlist-copy">
        <p class="index">002 / WAITLIST</p>
        <h2 id="waitlist-title">Segnala il tuo interesse</h2>
        <p>
          L'email serve a misurare una preferenza reale e, se il progetto passerà tutti
          i gate, ad avvisarti dell'eventuale evoluzione del test. Non viene effettuato
          alcun ordine.
        </p>
        <p class="selected-copy" id="selectedCopy">Seleziona prima uno dei tre prodotti.</p>
      </div>

      <form class="waitlist-form" id="dropWaitlistForm" method="post" action="/api/newsletter">
        <input type="hidden" name="campaign" value="drop-001" />
        <input type="hidden" name="creativeVersion" value={creativeVersion} />
        <input type="hidden" name="preference" id="preference" value="" />

        <div class="hp-field" aria-hidden="true">
          <label for="hp">Non compilare questo campo</label>
          <input id="hp" name="hp" type="text" tabindex="-1" autocomplete="off" />
        </div>

        <label for="email">Email</label>
        <input id="email" name="email" type="email" autocomplete="email" required placeholder="nome@email.it" />

        <label class="consent-row">
          <input type="checkbox" name="consent" required />
          <span>
            Accetto il trattamento dei dati secondo la <a href="/privacy-policy/">Privacy Policy</a>
            e l'iscrizione alla lista di aggiornamento Canapalandia.
          </span>
        </label>

        <button class="submit-button" type="submit" disabled>Entra nella waitlist</button>
        <p class="form-status" id="formStatus" role="status" aria-live="polite"></p>
      </form>
    </section>

    <section class="method" aria-labelledby="method-title">
      <p class="index">003 / GATE</p>
      <div>
        <h2 id="method-title">Cosa succede dopo il test</h2>
        <p>
          La waitlist non apre automaticamente la vendita. Un eventuale GO richiede
          ancora clearance IP del brand e del visual definitivo, chiusura dei gate
          fiscali/previdenziali, preflight Fourthwall e controllo compliance finale.
        </p>
      </div>
    </section>
  </main>
</Layout>

<script define:vars={{ creativeVersion }}>
  const buttons = Array.from(document.querySelectorAll(".interest-button"));
  const cards = Array.from(document.querySelectorAll("[data-product-card]"));
  const preference = document.querySelector("#preference");
  const selectedCopy = document.querySelector("#selectedCopy");
  const submitButton = document.querySelector(".submit-button");
  const form = document.querySelector("#dropWaitlistForm");
  const status = document.querySelector("#formStatus");

  function pushAnalytics(event, product, concept) {
    if (Array.isArray(window.dataLayer)) {
      window.dataLayer.push({ event, product, concept, creative_version: creativeVersion });
    }
  }

  function selectProduct(product, productName, concept) {
    if (!preference || !selectedCopy || !submitButton) return;

    preference.value = product;
    selectedCopy.textContent = `Hai scelto: ${productName}. Il visual mostrato è il placeholder neutro ${concept}.`;
    submitButton.disabled = false;

    cards.forEach((card) => {
      card.classList.toggle("is-selected", card.dataset.productCard === product);
    });

    buttons.forEach((button) => {
      const selected = button.dataset.product === product;
      button.setAttribute("aria-pressed", selected ? "true" : "false");
      button.textContent = selected ? "Formato selezionato" : "Mi interessa questo formato";
    });

    pushAnalytics("drop_001_product_interest", product, concept);
    document.querySelector("#waitlist")?.scrollIntoView({ behavior: "smooth", block: "start" });
  }

  buttons.forEach((button) => {
    button.setAttribute("aria-pressed", "false");
    button.addEventListener("click", () => {
      const product = button.dataset.product;
      const productName = button.dataset.productName;
      const concept = button.dataset.concept;
      if (product && productName && concept) selectProduct(product, productName, concept);
    });
  });

  form?.addEventListener("submit", async (event) => {
    event.preventDefault();
    if (!status || !preference || !preference.value || !submitButton) return;

    const data = new FormData(form);
    status.textContent = "Invio in corso…";
    submitButton.disabled = true;

    try {
      const response = await fetch("/api/newsletter", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          email: data.get("email"),
          consent: data.get("consent"),
          hp: data.get("hp"),
          campaign: "drop-001",
          preference: preference.value,
          creativeVersion,
        }),
      });

      const payload = await response.json().catch(() => ({ message: "Risposta non valida" }));
      if (!response.ok && response.status !== 409) {
        throw new Error(payload.message || "Errore durante l'iscrizione");
      }

      status.textContent = response.status === 409
        ? "Sei già nella lista. La preferenza è stata aggiornata."
        : "Grazie. Interesse registrato: nessun ordine è stato effettuato.";

      const selectedButton = buttons.find((button) => button.dataset.product === preference.value);
      pushAnalytics(
        "drop_001_waitlist_signup",
        preference.value,
        selectedButton?.dataset.concept || "",
      );
    } catch (error) {
      status.textContent = error instanceof Error ? error.message : "Errore durante l'iscrizione. Riprova.";
      submitButton.disabled = false;
    }
  });
</script>

<style>
  :global(#contenuto) {
    background: #f3efe3;
  }

  :global(#contenuto > .container.page-shell) {
    width: 100%;
    max-width: none;
    padding-left: 0;
    padding-right: 0;
  }

  .drop-page {
    --paper: #f3efe3;
    --surface: #fffdf7;
    --surface-soft: #e8ecdf;
    --ink: #172019;
    --muted: #4e584f;
    --line: rgba(23, 32, 25, 0.22);
    --line-strong: rgba(23, 32, 25, 0.42);
    --accent: #315c3c;
    width: min(1180px, calc(100% - 40px));
    margin: 0 auto;
    padding: clamp(42px, 7vw, 90px) 0 100px;
    color: var(--ink);
    font-family: "Archivo", Arial, sans-serif;
  }

  .drop-page :where(h1, h2, h3, p, label, span, strong, small) {
    color: inherit;
  }

  .drop-page a {
    color: #244f32 !important;
    text-decoration-thickness: 1px;
    text-underline-offset: 3px;
  }

  .drop-page a:hover {
    color: #153920 !important;
  }

  .kicker,
  .index,
  .product-eyebrow,
  .artwork-series,
  .artwork-foot {
    margin: 0;
    font-family: "Archivo", Arial, sans-serif;
    letter-spacing: 0.13em;
    text-transform: uppercase;
    font-size: 0.74rem;
    font-weight: 700;
  }

  .kicker,
  .index,
  .product-eyebrow {
    color: var(--accent);
  }

  .drop-hero {
    border-top: 1px solid var(--line-strong);
    border-bottom: 1px solid var(--line);
    padding: clamp(48px, 8vw, 96px) 0 clamp(42px, 6vw, 72px);
  }

  .drop-hero h1,
  .intro h2,
  .waitlist h2,
  .method h2 {
    font-family: "Newsreader", Georgia, serif;
    color: var(--ink);
  }

  .drop-hero h1 {
    margin: 15px 0 20px;
    font-size: clamp(4.6rem, 13vw, 10rem);
    line-height: 0.82;
    letter-spacing: -0.06em;
    font-weight: 600;
  }

  .lead {
    max-width: 820px;
    margin: 0;
    color: #28342b;
    font-family: "Newsreader", Georgia, serif;
    font-size: clamp(1.35rem, 2.7vw, 2.05rem);
    line-height: 1.3;
  }

  .status-note {
    max-width: 820px;
    margin-top: 30px;
    border-left: 4px solid var(--accent);
    background: var(--surface-soft);
    padding: 16px 18px;
    color: #354238;
    line-height: 1.6;
  }

  .status-note strong {
    color: var(--ink);
  }

  .intro,
  .method {
    display: grid;
    grid-template-columns: minmax(150px, 0.7fr) minmax(0, 2fr);
    gap: clamp(28px, 5vw, 60px);
    padding: clamp(50px, 6vw, 72px) 0;
    border-bottom: 1px solid var(--line);
  }

  .intro h2,
  .waitlist h2,
  .method h2 {
    margin: 0 0 16px;
    font-size: clamp(2.1rem, 4vw, 3.6rem);
    line-height: 1;
    letter-spacing: -0.035em;
  }

  .intro > div > p,
  .method p:last-child,
  .waitlist-copy > p:not(.index):not(.selected-copy) {
    max-width: 720px;
    margin: 0;
    color: var(--muted);
    font-size: 1.08rem;
    line-height: 1.72;
  }

  .launch-rule {
    max-width: 720px;
    margin-top: 24px;
    border: 1px solid var(--line-strong);
    background: var(--surface);
    padding: 16px 18px;
    color: #354238;
    line-height: 1.6;
  }

  .launch-rule strong {
    color: var(--ink);
  }

  .product-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 16px;
    padding: 28px 0;
    border-bottom: 1px solid var(--line);
  }

  .product-card {
    display: flex;
    flex-direction: column;
    min-width: 0;
    border: 1px solid var(--line);
    background: var(--surface);
    padding: 16px;
  }

  .product-card.is-selected {
    outline: 3px solid var(--accent);
    outline-offset: -3px;
  }

  .artwork {
    aspect-ratio: 4 / 5;
    background: #172019;
    color: #f6f1e4;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 20px;
  }

  .artwork :where(span, strong) {
    color: #f6f1e4;
  }

  .artwork strong {
    max-width: 10ch;
    font-family: "Archivo", Arial, sans-serif;
    font-size: clamp(2rem, 4vw, 4rem);
    line-height: 0.94;
    letter-spacing: -0.05em;
    font-weight: 700;
  }

  .artwork-foot {
    opacity: 0.84;
  }

  .product-body {
    display: flex;
    flex: 1;
    flex-direction: column;
    padding: 24px 3px 4px;
  }

  .product-body h3 {
    margin: 8px 0 12px;
    color: var(--ink);
    font-family: "Newsreader", Georgia, serif;
    font-size: 1.6rem;
    line-height: 1.08;
  }

  .pricing {
    margin: 14px 0 18px;
    padding: 14px 0;
    border-top: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }

  .reference-price {
    display: flex;
    justify-content: space-between;
    gap: 10px;
    margin: 0 0 7px;
    color: var(--muted);
    font-size: 0.82rem;
  }

  .reference-price strong {
    color: var(--ink);
    font-size: 0.96rem;
    font-weight: 600;
  }

  .launch-price {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 8px;
    margin: 0;
    color: var(--ink);
    font-size: 2rem;
    letter-spacing: -0.04em;
    font-weight: 700;
  }

  .launch-price span {
    color: var(--muted);
    font-size: 0.74rem;
    letter-spacing: 0;
    font-weight: 500;
  }

  .saving {
    display: inline-block;
    margin: 8px 0 0;
    padding: 5px 8px;
    background: var(--accent);
    color: #fffdf7 !important;
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
  }

  .product-note {
    min-height: 7em;
    margin: 0 0 18px;
    color: var(--muted);
    line-height: 1.62;
  }

  .interest-button,
  .submit-button {
    width: 100%;
    min-height: 46px;
    border: 1px solid var(--ink);
    background: transparent;
    color: var(--ink);
    padding: 13px 15px;
    cursor: pointer;
    font: inherit;
    font-weight: 700;
  }

  .interest-button {
    margin-top: auto;
  }

  .interest-button:hover,
  .interest-button[aria-pressed="true"],
  .submit-button:not(:disabled):hover {
    background: var(--ink);
    color: #fffdf7;
  }

  .interest-button:focus-visible,
  .submit-button:focus-visible,
  .waitlist-form input:focus-visible,
  .consent-row input:focus-visible {
    outline: 3px solid #8ca45e;
    outline-offset: 3px;
  }

  .waitlist {
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(320px, 0.95fr);
    gap: clamp(36px, 7vw, 92px);
    margin: 34px 0 0;
    padding: clamp(38px, 6vw, 64px);
    border: 1px solid var(--line-strong);
    background: var(--surface-soft);
  }

  .selected-copy {
    margin: 25px 0 0;
    color: var(--ink);
    font-weight: 700;
  }

  .waitlist-form {
    display: grid;
    gap: 12px;
    align-content: start;
    border: 1px solid var(--line);
    background: var(--surface);
    padding: clamp(20px, 3vw, 30px);
  }

  .waitlist-form > label:not(.consent-row) {
    color: var(--ink);
    font-size: 0.84rem;
    font-weight: 700;
  }

  .waitlist-form input[type="email"] {
    width: 100%;
    box-sizing: border-box;
    border: 1px solid var(--line-strong);
    background: #ffffff;
    color: var(--ink);
    padding: 14px;
    font: inherit;
  }

  .waitlist-form input[type="email"]::placeholder {
    color: #707970;
  }

  .consent-row {
    display: grid;
    grid-template-columns: 20px 1fr;
    gap: 9px;
    align-items: start;
    margin: 8px 0 3px;
    color: var(--muted);
    font-size: 0.84rem;
    line-height: 1.5;
  }

  .consent-row a {
    color: #244f32 !important;
    font-weight: 700;
  }

  .submit-button {
    background: var(--ink);
    color: #fffdf7;
  }

  .submit-button:disabled {
    cursor: not-allowed;
    opacity: 0.65;
  }

  .form-status {
    min-height: 1.4em;
    margin: 0;
    color: var(--ink);
    font-size: 0.9rem;
  }

  .hp-field {
    position: absolute;
    left: -10000px;
    width: 1px;
    height: 1px;
    overflow: hidden;
  }

  .method {
    margin-top: 34px;
    border-top: 1px solid var(--line);
  }

  @media (max-width: 900px) {
    .product-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .product-card {
      display: grid;
      grid-template-columns: minmax(180px, 0.75fr) minmax(0, 1.25fr);
      gap: 22px;
    }

    .product-body {
      padding-top: 8px;
    }

    .product-note {
      min-height: 0;
    }
  }

  @media (max-width: 760px) {
    .drop-page {
      width: min(100% - 28px, 1180px);
      padding-top: 34px;
      padding-bottom: 70px;
    }

    .intro,
    .method,
    .waitlist {
      grid-template-columns: 1fr;
    }

    .waitlist {
      padding: 26px 20px;
    }
  }

  @media (max-width: 560px) {
    .drop-hero h1 {
      font-size: clamp(4rem, 23vw, 6rem);
    }

    .product-card {
      display: block;
    }

    .product-body {
      padding-top: 22px;
    }

    .artwork {
      aspect-ratio: 5 / 4;
    }
  }
</style>
