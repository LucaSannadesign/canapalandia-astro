---
import { commerceConfig, isSpreadshopEmbedReady } from "../../config/commerce";
import SpreadshopEmbed from "./SpreadshopEmbed.astro";

type Props = {
  startToken?: string;
};

const { startToken } = Astro.props;
---

{commerceConfig.provider === "spreadshop" && isSpreadshopEmbedReady ? (
  <SpreadshopEmbed startToken={startToken} />
) : null}
